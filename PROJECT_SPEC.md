# 航天器热真空试验工况编排与放行闭环

## 项目目标

建设一个面向航天器部组件热真空鉴定试验的 Go HTTP 后端。服务围绕“试件—试验代次—工况阶段—测点证据”组织不可变计划、入舱基线、抽真空、冷热循环、稳态保持、异常处置、复验和唯一终局。温度、压力、时间和变化率均使用定点整数及明确舍入；步骤通过事务事件、幂等键、设备租约和逻辑时钟推进。SQLite 或等价嵌入式事务数据库保存计划、事件、测点、异常与裁定，进程重启后恢复未完成试验并使过期租约失效。生产实现须含20—30个Go文件、至少4个职责明确的包、2000—3000有效生产行且目标约2500行；测试不少于4个公开测试文件和12个公开用例。仓库包含go.mod、服务入口、迁移与恢复逻辑，以及支持linux/amd64和linux/arm64构建的Dockerfile。

## 端到端业务流程

1. 计划锁定：录入试件标识、传感器布点、真空阈值、冷热目标、升降温速率、稳态窗口、循环次数、设备校准摘要和步骤依赖；验证后生成不可修改的计划版本。
2. 入舱建基线：以幂等请求取得舱体和采集器租约，登记安装检查、传感器零点读数、舱门封闭及初始压力；全部基线证据合格后才允许抽真空。
3. 工况执行：依次完成抽真空、低温降温、低温稳态、高温升温、高温稳态及规定循环。测点读数按逻辑时间追加，系统在每个稳态窗口计算覆盖率、极差和漂移率，满足条件后才能推进。
4. 异常复验：传感器失联、压力回升、速率越限或测点超差会冻结当前步骤；依据测点分组、共享采集器和受影响工况确定复验集合，原子创建新代次，旧代次迟到读数只归档。
5. 终局放行：完成全部循环、回常温和复压后，两名不同资格人员签署同一证据摘要；仲裁器仅能提交出舱放行、继续隔离或试验终止中的一个终局及唯一凭据。

## 核心组件与职责

1. 试验计划目录：校验并锁定试件、测点、阈值、设备校准摘要、工况阶段和依赖图，拒绝重复测点、依赖环及无法覆盖的阶段。
2. 试验聚合与持久化：以只追加事件维护当前代次、阶段状态、循环前缀、有效证据索引和终局屏障；负责数据库事务、幂等结果及重启恢复。
3. 工况与测量引擎：编排抽真空、温度循环、稳态窗口、回常温和复压，以定点整数计算速率、极差、漂移及证据覆盖率。
4. 设备租约与采集适配器：管理舱体、温控器、真空计和采集器的限时互斥租约；通过脚本化适配器产生成功、超时、断连、校准过期或格式错误结果。
5. 异常复验与终局仲裁器：生成规范化复验集合、隔离迟到证据，检查循环闭合、环境恢复、双人复核，并原子写入唯一终局。
6. HTTP API：提供JSON契约、稳定错误码、请求摘要、确定排序、健康检查和并发控制；公开测试可用httptest、临时数据库和逻辑时钟完成。

## 领域规则与不变量

1. 锁定计划的阶段依赖必须无环；每个循环必须具有连续编号，且抽真空、冷热稳态、回常温、复压只能按锁定依赖推进。
2. 温度使用毫开尔文、压力使用毫帕、时间使用毫秒、比率使用百万分比；所有乘加、差值和除法必须检查溢出、负间隔、除零，并按对合格判定不利的方向舍入。
3. 测量证据必须绑定计划版本、试验代次、阶段、循环、测点、设备、校准摘要及单调逻辑时间；旧代次、过期租约或时间倒退的结果不得进入当前有效索引。
4. 稳态合格要求窗口覆盖率、每个必需测点的样本数量、温度极差、漂移率和舱压同时达标；缺测、重复时间点或窗口外读数不能补足覆盖率。
5. 同一试验的循环只能形成从1开始的连续完成前缀；阶段推进、证据索引和租约状态必须在同一事务提交，任一校验失败整体回滚。
6. 异常影响集合为异常测点、同组测点、共享采集器测点及锁定规则指定的相关阶段之闭包，按阶段序号、循环、测点排序去重；相同异常事实只能创建一个下一代次。
7. 每个幂等键绑定规范化请求摘要；同键同内容返回原结果，同键异内容返回稳定冲突码。设备失败只追加确定序号的尝试记录，不推进阶段或解除冻结。
8. 终局要求全部循环闭合、当前代次无未完成复验、舱内已回常温并安全复压、证据摘要完整，以及两名不同且资格有效的人员独立签署；终局一经提交不可替换。

## 数据模型与持久化

1. TestPlan、StageSpec、SensorSpec：计划版本、试件、阶段依赖、循环数量、测点分组、定点阈值、设备与校准摘要。
2. TestRun、RunEvent、CycleState：当前代次、阶段、循环前缀、冻结原因、事件序号和恢复检查点。
3. Equipment、Lease、MeasurementCall：设备类型、租约令牌、持有者、逻辑有效期、调用尝试、故障类型及原始载荷摘要。
4. Measurement、EvidenceWindow：测点整数读数、逻辑时间、代次、阶段、循环、有效性，以及窗口覆盖率、极差、漂移率和判定。
5. Anomaly、RetestGeneration：异常事实摘要、传播依据、排序后的影响集合、新代次和覆盖进度。
6. Review、FinalVerdict、IdempotencyRecord：复核者资格、签名摘要、终局类型、唯一凭据、请求摘要、响应和提交事件序号。

## 公开接口

1. POST /v1/plans/lock 锁定计划；GET /v1/plans/{id}读取不可变计划。
2. POST /v1/runs 创建试验；GET /v1/runs/{id}/state按阶段、循环和测点稳定排序返回恢复状态。
3. POST /v1/runs/{id}/baseline、/stages/{stage}/start、/measurements、/stages/{stage}/complete 推进入舱基线和工况阶段。
4. POST /v1/equipment/{id}/leases、/renew、/release管理租约；GET /v1/measurement-calls/{id}读取确定性重试状态。
5. POST /v1/runs/{id}/anomalies创建复验代次；GET /v1/runs/{id}/retests/current读取影响集合与未完成项。
6. POST /v1/runs/{id}/reviews登记复核；POST /v1/runs/{id}/verdicts竞争终局；GET /v1/runs/{id}/verdict读取唯一结果。所有写接口要求Idempotency-Key。

## 失败边界

1. 计划边界：依赖环、测点重复、阈值非法、校准摘要缺失或整数范围错误在锁定前拒绝，不产生计划版本。
2. 事务边界：阶段推进、循环完成、证据索引、租约变化和幂等响应共同提交；写冲突或规则失败不留下部分状态。
3. 采集边界：超时、断连、校准过期、格式错误和过期令牌仅形成失败调用；重试次数由已持久化尝试确定。
4. 代次边界：新复验代次与影响集合原子创建；并发创建只接受一个，旧代次迟到读数不能解除当前冻结。
5. 恢复边界：启动时重放已提交事件、校验检查点并按持久化逻辑时间使租约失效；未提交事务不可见。
6. 终局边界：三种终局共享唯一约束和比较交换版本；并发请求只能提交一个凭据，失败方不得留下半完成复核或裁定。

## 验收标准

1. 可锁定包含测点、设备、阈值、阶段依赖和循环次数的不可变计划，并确定拒绝依赖环、重复测点、非法范围与陈旧摘要。
2. 定点算法可精确验证稳态覆盖率、极差、漂移率、温压速率及边界舍入，并稳定拒绝溢出、除零和非正时间间隔。
3. 各循环只能形成连续完成前缀；缺失基线、跳过阶段、窗口证据不足、错误代次或过期租约均不得推进有效状态。
4. 并发租约与阶段提交满足原子性；幂等同内容只执行一次，异内容返回冲突且数据库无部分修改。
5. 脚本化采集失败仅增加确定的尝试记录，不伪造读数或解除冻结；成功重试在服务重启后可继续原流程。
6. 异常能够生成唯一且稳定排序的复验集合；并发代次创建只有一个成功，旧代次迟到读数保留但不改变当前结论。
7. 重启后恢复计划、阶段、循环前缀、测量证据、异常代次、租约、幂等响应和终局；故障注入下无半提交事件。
8. 仅在环境恢复、循环与复验闭合、证据齐全及两名独立合格复核者签署后生成唯一凭据；并发终局竞争只有一个成功。

## 确定性测试场景

1. 锁定三阶段两循环计划，断言依赖环、重复测点和陈旧校准摘要返回固定错误码及稳定原因顺序。
2. 用阈值两侧整数读数验证温度极差、漂移率和覆盖率的不利舍入，并验证溢出、除零和时间倒退拒绝。
3. 两协程竞争同一舱体租约，仅一个成功；失败事务不创建阶段事件。
4. 提交窗口边界内外及重复时间点读数，精确断言样本数量、覆盖率与稳态结论。
5. 尝试跳过抽真空、直接完成高温稳态或提交循环3，断言循环前缀与有效证据索引不变。
6. 脚本化采集器依次超时、格式错误、成功，断言尝试序号为1、2、3且仅第三次产生有效测量。
7. 租约过期并被新持有者取得后，以旧令牌提交读数，断言读数仅记录为拒绝事件。
8. 由单测点失联触发同组和共享采集器闭包，断言复验集合的内容、去重和排序完全固定。
9. 创建新代次后提交旧代次合格窗口，断言只进入历史；并发创建下一代次只成功一次。
10. 相同幂等键并发提交同一阶段完成请求，断言只产生一个事件；更换内容后返回幂等冲突。
11. 在事件写入与检查点更新之间注入事务故障并重启，断言两者均未出现，重放请求可成功。
12. 同一人员重复复核被拒；两名合格人员复核后，并发申请三种终局，数据库中仅有一个稳定凭据。

## 组件追踪关系

1. 试验计划目录覆盖计划锁定流程、计划模型与首项验收，并由依赖环和测点校验场景验证。
2. 试验聚合与持久化覆盖连续循环、事务幂等、恢复及终局屏障，对应验收3、4、7、8。
3. 工况与测量引擎覆盖基线、温压循环、稳态窗口及定点规则，对应验收2、3及窗口边界测试。
4. 设备租约与采集适配器覆盖互斥占用、失败重试和过期令牌，对应验收4、5及租约竞争测试。
5. 异常复验与终局仲裁器覆盖影响闭包、代次隔离、独立复核和唯一裁定，对应验收6、8。
6. HTTP API贯穿全部流程，落实JSON契约、稳定错误码、确定排序与公开可测试性；至少4个公开测试文件承载不少于12个独立测试。

## 独特性

项目以航天器热真空鉴定中的温压工况依赖、稳态证据窗口、测点传播闭包和试验代次隔离为核心，将设备互斥、定点判据、迟到遥测与双人放行统一到可恢复的单一试验闭环中。
